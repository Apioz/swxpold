import { Empty } from 'antd';
import './SecurityPages.css';

export default function EventWorkbench() {
  return (
    <div className="security-table-card" style={{ minHeight: 400 }}>
      <Empty description="事件工作台 — 内容待后续补充" />
    </div>
  );
}
